# Core RL training (verl)
# Install verl from local repo: pip install -e ../verl
torch>=2.4.0
transformers>=4.45.0
datasets
accelerate
vllm>=0.6.0
ray>=2.10
omegaconf
hydra-core

# SAE interpretability — installed from local repo:
#   pip install -e C:\Users\OhhMoo\Desktop\git_repo\SAE_Tools\SAELens
# (SAELens v6.39.0, includes transformer-lens as a dependency)

# Experiment tracking & utilities
wandb
numpy
pandas
matplotlib
seaborn
tqdm
